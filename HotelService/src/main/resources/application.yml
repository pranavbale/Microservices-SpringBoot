server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Microservices-HotelService
    username: postgres
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update